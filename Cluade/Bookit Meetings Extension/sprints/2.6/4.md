In bookit-meetings/dashboard/src/views/BookingDetailView.vue, make exactly this one change:
Move these three lines from inside <script setup> to above the <script setup> block, at module level:
jsconst activeBookingId = ref(null)
const loading = ref(false)
const booking = ref(null)
The imports (import { ref, computed, onMounted, onUnmounted } from 'vue' and import MeetingInfoPanel) must also move to module level above <script setup>.
The result should look like this at the top of the file:
jsimport { ref, computed, onMounted, onUnmounted } from 'vue'
import MeetingInfoPanel from '../components/MeetingInfoPanel.vue'

// Module-level state — persists across component remounts
const activeBookingId = ref(null)
const loading = ref(false)
const booking = ref(null)

<script setup>
// computed, patchFetchOnce, startModalCloseObserver, onMounted, onUnmounted all stay here
Do not change anything else in this file.
No PHPUnit run needed — Vue only.