# TASK 1 OF 4: Verify environment against core v1.5.1
Sprint: Pre-Sprint 3 Housekeeping | Est: 30m | Plugin root: bookit-meetings/

---

## READ FIRST — DO NOT WRITE ANY CODE UNTIL YOU HAVE READ THESE FILES

1. `.wp-env.json` (repo root) — confirm the core plugin is referenced as `../bookit-booking-system` (local folder path); confirm no edits are needed
2. `../bookit-booking-system/bookit-booking-system.php` — confirm the version string reads 1.5.1 before proceeding; if it does not, stop and report back

If any file does not exist, stop and report back before proceeding.

---

## CONTEXT

The v1.5.1 core plugin folder has already been copied to `../bookit-booking-system`. The `.wp-env.json` references that folder by local path and does **not** need to be edited. This task restarts the wp-env environment against the updated core, confirms clean extension activation, and verifies the PHPUnit baseline is intact before housekeeping Tasks 2–4 proceed.

---

## IMPLEMENTATION REQUIREMENTS

### `.wp-env.json` — NO CHANGES

Do not edit this file. The path `../bookit-booking-system` already points to the v1.5.1 folder.

### Environment — VERIFY AND RESTART

Perform these steps in order:

1. Confirm `../bookit-booking-system/bookit-booking-system.php` declares version 1.5.1 — if not, stop and report back


---

## INFRASTRUCTURE WIRING

No new infrastructure in this task. Confirm existing wiring is intact after the core upgrade:

- [ ] Migration runner activates cleanly — `bookit_register_migration_path` hook fires without error
- [ ] `0001-add-meetings-schema.php` migration runs (or is skipped as already applied) without fatal errors
- [ ] Extension deactivation and reactivation cycle completes without PHP errors

---

## PHPUNIT REQUIREMENTS

Baseline: **51 tests, 107 assertions, 0 failures** — must not regress.

No new tests are written in this task.

Run after the environment is restarted and the plugin is reactivated:

```
wp-env run tests vendor/bin/phpunit
```

**All 51 tests must pass, all 107 assertions must be confirmed, 0 failures — before this task is marked complete.**

Note: A `class_alias()` block still exists in `0001-add-meetings-schema.php`. Under v1.5.1 this alias is inert (the runner no longer needs it) but it is harmless. Do NOT remove it in this task — Task 2 handles that removal.

---

## ACCEPTANCE CRITERIA

### Functional
- [ ] `../bookit-booking-system/bookit-booking-system.php` version string confirms 1.5.1
- [ ] `wp-env restart` completes without error
- [ ] Deactivating and reactivating the Bookit Meetings extension produces no PHP warnings, notices, or fatal errors
- [ ] WordPress debug log is clean — no new entries from the extension on activation

### Technical
- [ ] No changes made to any file in this task
- [ ] PHPUnit suite passes: **51 tests, 107 assertions, 0 failures**

### Must NOT break
- [ ] Core plugin (v1.5.1) activates and functions normally alongside the extension
- [ ] Extension deactivates cleanly with no errors
- [ ] Existing migration schema (`{prefix}bookings` `meeting_link` column) is present and unaffected

---

## GIT COMMIT MESSAGE

No commit required for this task — no files were changed.

---

If you encounter an architecture decision not covered above, or a conflict with existing code that this prompt does not resolve, STOP and report back before writing any code.