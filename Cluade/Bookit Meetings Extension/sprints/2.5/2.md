# TASK 2 OF 4: Fix `migration_id()` and remove `class_alias()` from migration file
Sprint: Pre-Sprint 3 Housekeeping | Est: 30m | Plugin root: bookit-meetings/

---

## READ FIRST — DO NOT WRITE ANY CODE UNTIL YOU HAVE READ THESE FILES

1. `bookit-meetings/database/migrations/0001-add-meetings-schema.php` — the only file being changed; read it in full before touching anything
2. `../bookit-booking-system/includes/class-bookit-migration-runner.php` — read `find_migration_class()` and the `run_pending()` loop to confirm how `$migration_id` is derived from the filename and matched against `migration_id()`

If any file does not exist, stop and report back before proceeding.

---

## CONTEXT

Core v1.5.1's migration runner derives `$migration_id` from the filename using `pathinfo( $file, PATHINFO_FILENAME )`, which for `0001-add-meetings-schema.php` produces `'0001-add-meetings-schema'`. It then calls `find_migration_class()`, which scans all declared classes and matches on `$instance->migration_id() === $expected_migration_id`. The extension migration class currently returns `'meetings-0001-add-meetings-schema'` from `migration_id()` — the `meetings-` prefix causes the match to fail and the migration to error. The `class_alias()` block at the bottom of the file was a workaround for the old filename-based runner and is no longer needed. Both issues are fixed in this task. No other files change.

There is no stale tracking row in `{prefix}bookings_migrations` for this migration — the migration has never successfully run — so no DB cleanup is required.

---

## IMPLEMENTATION REQUIREMENTS

### `bookit-meetings/database/migrations/0001-add-meetings-schema.php` — MODIFY

Make exactly two changes and nothing else:

**Change 1 — Fix `migration_id()`:**

Replace the current return value:
```php
public function migration_id(): string {
    return 'meetings-0001-add-meetings-schema';
}
```

With:
```php
public function migration_id(): string {
    return '0001-add-meetings-schema';
}
```

This must exactly match what `pathinfo( '0001-add-meetings-schema.php', PATHINFO_FILENAME )` returns — i.e. the filename without extension, with no prefix added.

**Change 2 — Remove the `class_alias()` block:**

Remove this entire block at the bottom of the file (including the comment line):
```php
// Keep migration runner compatibility (expects class name derived from filename).
if ( ! class_exists( 'Bookit_Migration_0001_Add_Meetings_Schema' ) ) {
    class_alias( 'Bookit_Migration_Meetings_0001_Add_Meetings_Schema', 'Bookit_Migration_0001_Add_Meetings_Schema' );
}
```

Do not change anything else in the file — not the class name, not `plugin_slug()`, not `up()`, not `down()`.

---

## INFRASTRUCTURE WIRING

- [ ] `migration_id()` now returns `'0001-add-meetings-schema'` — matches filename exactly
- [ ] `plugin_slug()` still returns `'bookit-meetings'` — unchanged
- [ ] `class_alias()` block removed — file ends after the closing `}` of the class

---

## VERIFICATION STEPS

After making the two changes:

1. In the wp-env dev site (or your local site), **deactivate** the Bookit Meetings extension plugin, then **reactivate** it
2. Check the debug log — confirm you see a successful migration log entry, NOT the `"No migration class found"` error
3. Confirm the `meeting_link` column now exists on `{prefix}bookings` and the `{prefix}bookit_meetings_credentials` table was created
4. Confirm a row for `migration_id = '0001-add-meetings-schema'` and `plugin_slug = 'bookit-meetings'` now exists in `{prefix}bookings_migrations`
5. Run PHPUnit — the two previously skipped tests should now execute because `meeting_link` exists in the tests DB

---

## PHPUNIT REQUIREMENTS

Baseline: **51 tests, 107 assertions, 0 failures** — must be fully restored in this task.

The two tests that were skipping (`test_booking_response_filter_adds_meeting_link` and `test_booking_response_filter_empty_string_when_no_link` in `tests/unit/test-backend-wiring.php`) were skipping because `meeting_link` was absent from the tests DB. After this fix the migration will run in the tests environment and those tests must execute and pass.

Run after deactivate/reactivate:
```
wp-env run tests vendor/bin/phpunit
```

**Required result: 51 tests, 107 assertions, 0 failures, 0 skipped.**

If the count is still 104 assertions with 2 skips after the migration fix, it means the tests environment DB did not pick up the migration — destroy and recreate the tests environment:
```
wp-env destroy
wp-env start
```
Then reactivate the extension and run PHPUnit again.

---

## ACCEPTANCE CRITERIA

### Functional
- [ ] Deactivating and reactivating the extension produces no `"No migration class found"` error in the debug log
- [ ] `{prefix}bookings` table has a `meeting_link` column after activation
- [ ] `{prefix}bookit_meetings_credentials` table exists after activation
- [ ] `{prefix}bookings_migrations` contains a row: `migration_id = '0001-add-meetings-schema'`, `plugin_slug = 'bookit-meetings'`

### Technical
- [ ] `migration_id()` returns exactly `'0001-add-meetings-schema'`
- [ ] `plugin_slug()` still returns `'bookit-meetings'`
- [ ] `class_alias()` block is fully removed — no dead code remains
- [ ] No other lines in the file were changed
- [ ] PHPUnit suite passes: **51 tests, 107 assertions, 0 failures, 0 skipped**

### Must NOT break
- [ ] Core plugin activates and functions normally alongside the extension
- [ ] Extension deactivates cleanly — `down()` runs without error (verify via deactivation in wp-env)
- [ ] All core migrations remain unaffected

---

## GIT COMMIT MESSAGE

```
Pre-Sprint 3 Housekeeping, Task 2: Fix migration_id() and remove class_alias()

- Fixed migration_id() return value: 'meetings-0001-add-meetings-schema'
  → '0001-add-meetings-schema' to match filename-based runner lookup
- Removed class_alias() workaround — no longer needed in core v1.5.1
- Migration now runs cleanly on extension activation

Tests: 51 passing, 107 assertions, 0 failures, 0 skipped
```

---

If you encounter an architecture decision not covered above, or a conflict with existing code that this prompt does not resolve, STOP and report back before writing any code.