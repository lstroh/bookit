Here is the sprint completion prompt — steps 1, 2, and 3:

---

```
SPRINT 7 COMPLETION: Version bump to v1.5.1 + final test run
Plugin root: bookit-booking-system/
Branch: Phase2
PHPUnit baseline: 985 tests, 0 failures
```

---

## READ FIRST — DO NOT WRITE ANY CODE UNTIL YOU HAVE READ THESE FILES

1. `bookit-booking-system.php` — the main plugin file; find the plugin header comment (`Version:`) and any `define( 'BOOKIT_VERSION', ... )` or equivalent version constant
2. Search the codebase for any other files that define a version constant (e.g. `BOOKIT_VERSION`) — there may be one in a constants file or at the top of the main plugin file only; read every location before changing anything

If the version string appears in more than one place, update all of them.

---

## IMPLEMENTATION REQUIREMENTS

### Version bump — all locations

Update every occurrence of the version from its current value to `1.5.1`:

- Plugin header `Version:` field in `bookit-booking-system.php`
- Any `define( 'BOOKIT_VERSION', '...' )` constant
- Any other version constant in any file that currently holds the old version string

Do **not** change anything else. This is a version-string-only update.

---

## PHPUNIT REQUIREMENTS

Run the full test suite after the version bump:

```bash
cd bookit-booking-system && vendor/bin/phpunit
```

**Required result:** 985 tests, 0 failures.

Report the exact output line (e.g. `OK (985 tests, 2831 assertions)`).

---

## GIT COMMIT

Stage all changed files and commit with exactly this message:

```
Sprint 7 complete: v1.5.1 — Extension API improvements

- Add bookit_staff_email_meeting_section filter (Task 1)
- Add bookit_dashboard_extension_content action (Task 2)
- Add bookit_schedule_booking_response filter (Task 3)
- Add data-booking-id to MySchedule.vue cards (Task 4)
- Migration runner class lookup by migration_id() (Task 5)
- Re-fetch booking after bookit_after_booking_confirmed (Task 6)
- Fix bookit_booking_response spec signature (Task 7)

Tests: 985 passing, 0 failures
```

Do **not** push — push and tagging will be handled manually.

---

## ACCEPTANCE CRITERIA

- [ ] `Version:` header in `bookit-booking-system.php` reads `1.5.1`
- [ ] `BOOKIT_VERSION` constant (and any other version constant) reads `1.5.1`
- [ ] PHPUnit: 985 tests, 0 failures
- [ ] Git commit created with the exact message above
- [ ] Nothing pushed

---

If the version string is found in more than the expected locations, list all of them before making any changes.