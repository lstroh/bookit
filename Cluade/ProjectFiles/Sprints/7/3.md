Here is Task 3:

---

```
TASK 3 OF 7: Add bookit_schedule_booking_response filter
Sprint: 7 | Est: ~30min | Plugin root: bookit-booking-system/
Branch: Phase2
PHPUnit baseline: 980 tests, 0 failures
```

---

## READ FIRST — DO NOT WRITE ANY CODE UNTIL YOU HAVE READ THESE FILES

1. `includes/api/class-dashboard-bookings-api.php` — read the entire file; locate and read both `format_booking()` and `format_schedule_booking()` in full; find the existing `bookit_booking_response` filter in `format_booking()` and understand its exact pattern before writing anything

If the file does not exist at that path, stop and report back before proceeding.

---

## CONTEXT

`format_booking()` already applies `apply_filters( 'bookit_booking_response', $formatted, (int) $row['id'] )` before returning, letting extensions append fields to booking API responses. The schedule endpoint uses a separate `format_schedule_booking()` method with no equivalent hook — extensions cannot add fields (e.g. `meeting_link`) to schedule responses. This task adds the identical pattern to `format_schedule_booking()`.

---

## IMPLEMENTATION REQUIREMENTS

### `includes/api/class-dashboard-bookings-api.php` — MODIFY

At the end of `format_schedule_booking()`, replace the bare `return array( ... )` with:

1. Assign the array to `$formatted`
2. Apply the filter
3. Return `$formatted`

```php
$formatted = array(
    // ... all existing fields unchanged ...
);

$formatted = apply_filters(
    'bookit_schedule_booking_response',
    $formatted,
    (int) $row['id']
);

return $formatted;
```

Follow the `bookit_booking_response` pattern in `format_booking()` exactly — same variable name `$formatted`, same `(int)` cast on the ID, same position immediately before `return`.

**No other changes** to the file. Do not touch `format_booking()` or `bookit_booking_response`.

---

## PHPUNIT REQUIREMENTS

Baseline: 980 tests, 0 failures — must not regress.

Add to `tests/unit/test-sprint7-extension-api.php`:

Required test cases:

- `test_schedule_booking_response_filter_fires_with_correct_params` — hook fires with two params: `array $formatted` and `int $booking_id`; verify `$booking_id` is an integer
- `test_schedule_booking_response_filter_can_add_fields` — when the filter adds a field (e.g. `meeting_link`) to the response array, that field is present in the value returned by `format_schedule_booking()`
- `test_schedule_booking_response_filter_must_return_array` — all existing fields from the original array are still present in the return value when no filter modifies anything

Run after implementation:

```bash
cd bookit-booking-system && vendor/bin/phpunit
```

All tests must pass before marking this task complete.

---

## ACCEPTANCE CRITERIA

### Functional
- [ ] `bookit_schedule_booking_response` filter fires at the end of `format_schedule_booking()`
- [ ] Filter receives `array $formatted` as first param and `int $booking_id` as second
- [ ] The return value of the filter is what the method returns
- [ ] Extensions can add or modify fields in the schedule booking response

### Technical
- [ ] No PHP warnings or notices
- [ ] Follows `bookit_booking_response` pattern in `format_booking()` exactly
- [ ] PHPUnit suite passes (980+ tests, 0 failures)

### Must NOT break
- [ ] `GET /dashboard/my-schedule` response — all existing fields present when no extension hooks the filter
- [ ] `format_booking()` and `bookit_booking_response` — separate method, must not be touched

---

## GIT COMMIT MESSAGE

```
Sprint 7, Task 3: Add bookit_schedule_booking_response filter to format_schedule_booking()

- Mirrors bookit_booking_response pattern from format_booking()
- Filter fires with ($formatted array, $booking_id int) before return
- Allows extensions to add fields to schedule booking API responses
- PHPUnit: [N] tests, 0 failures
```

---

If you encounter an architecture decision not covered above, or a conflict with existing code that this prompt does not resolve, **STOP and report back before writing any code.**